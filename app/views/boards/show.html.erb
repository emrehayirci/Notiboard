<% if flash[notice] %>

<%end %>

<div class="jumbotron">
  <h1> <%= @board.title %> </h1>
  <p> <%= @board.description%> </p>
  <p><a class="btn btn-primary btn-lg" href="#" role="button">UYE OL!</a></p>
</div>
<%= link_to "Edit Board" , patch_board_path(@board.id), class: "btn btn-primary btn-lg"%>
